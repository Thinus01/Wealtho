<!DOCTYPE html>
<html>
  <head>
    <title>Wealtho</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if current_user %>
      <%= yield %>
      <%= render 'layouts/loginpage' %>
    <% else %>
    <%= yield %>
      <%= render 'layouts/loginpage' %>
    <% end %>
  </body>
</html>
